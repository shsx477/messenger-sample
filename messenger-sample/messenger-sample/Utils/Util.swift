import Foundation
import SwiftUI

class Util {
  static let tempIconName = "questionmark.square"
  static let iconName_xmark = "xmark"
  static let defaultBgColor = Color(red: 133 / 255, green: 146 / 255, blue: 153 / 255)
  static let tempProfileImageData = UIImage(named: "user-default")!.pngData()!
}
